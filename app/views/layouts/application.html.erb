<!DOCTYPE html>
<html>
	<!-- <p class="notice"><%= notice %></p> -->
	<!-- <p class="alert"><%= alert %></p> -->

<head>
  <title>归来兮</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
	<div >
		<% if user_signed_in? %>
			你好:<%= current_user.email %>
			| <%= link_to('Logout', destroy_user_session_path, :method => :delete) %> |
			  <%= link_to('EditMyInfo', edit_user_registration_path(current_user)) %>
			| <%= link_to('My Items',items_path) %> 
			| <%= link_to('Home',shiyi_path) %>
		<% else %>
			<%= link_to "注册",new_user_registration_path %> | <%= link_to "登陆", new_user_session_path %>
		<% end %>
	</div>
<%= yield %>

</body>
</html>
